# Copier vers /etc/etoro/interface.env puis : sudo chmod 600 /etc/etoro/interface.env
# Référencer ce fichier dans les unités systemd (Gunicorn + sync-trader-posts).
# Ne pas commiter de fichier rempli avec de vraies clés.

ETORO_API_KEY=
ETORO_USER_KEY=
